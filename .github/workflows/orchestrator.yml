name: Issue Orchestrator

# 支持三种触发方式：@mention、/command、标签变更
on:
  issue_comment:
    types: [created, edited]
  issues:
    types: [labeled]

# 并发控制：同一 Issue 不会并发运行
concurrency:
  group: ${{ github.event.issue.number }}
  cancel-in-progress: true

permissions:
  issues: write
  contents: read

jobs:
  # ========== 标签触发：state:ready-for-review ==========
  auto-trigger-review:
    if: github.event_name == 'issues' &&
        github.event.action == 'labeled' &&
        github.event.label.name == 'state:ready-for-review'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync

      - name: Cache arxiv-mcp-server
        uses: actions/cache@v4
        with:
          path: ~/.local/share/uv/tools/arxiv-mcp-server
          key: arxiv-mcp-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            arxiv-mcp-${{ runner.os }}-

      - name: Run full review process
        run: |
          echo "🏷️ 标签触发评审开始"
          gh issue edit ${{ github.event.issue.number }} \
            --add-label "state:review" \
            --remove-label "state:ready-for-review" \
            --repo ${{ github.repository }}

          echo "🚀 Running review flow..."
          uv run python -m issuelab review \
            --issue ${{ github.event.issue.number }} \
            --post

          echo "📝 标记需要总结..."
          gh issue edit ${{ github.event.issue.number }} \
            --add-label "bot:needs-summary" \
            --repo ${{ github.repository }}

          echo "✅ 评审完成"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_AUTH_KEY: ${{ secrets.ANTHROPIC_AUTH_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          ARXIV_STORAGE_PATH: ${{ github.workspace }}/.arxiv-papers
          LOG_FILE: ${{ github.workspace }}/logs/auto_review_${{ github.event.issue.number }}.log
          LOG_LEVEL: DEBUG
          ENABLE_ARXIV_MCP: true
          ENABLE_GITHUB_MCP: false
          # SDK 优化：跳过版本检查，节省 2 秒
          CLAUDE_AGENT_SDK_SKIP_VERSION_CHECK: "true"
          # SDK 优化：减少等待时间
          CLAUDE_CODE_STREAM_CLOSE_TIMEOUT: "30000"

      # 上传日志文件
      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: auto-review-logs-${{ github.event.issue.number }}-${{ github.run_id }}
          path: logs/
          retention-days: 7
          if-no-files-found: ignore

  # ========== @mention 触发（并行执行） ==========
  process-mention:
    if: github.event_name == 'issue_comment' &&
        contains(github.event.comment.body, '@')
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync

      # 检查 mention 是否为内置 Agent
      - name: Check for built-in agents
        id: check-agents
        run: |
          COMMENT="${{ github.event.comment.body }}"

          # 定义内置 Agent 映射（@Mention 别名 -> agent name）
          # 与 parser.py 中的 AGENT_ALIASES 保持一致
          declare -A AGENT_MAP=(
            ["moderator"]="moderator"
            ["mod"]="moderator"
            ["reviewer_a"]="reviewer_a"
            ["reviewera"]="reviewer_a"
            ["reviewer"]="reviewer_a"
            ["reva"]="reviewer_a"
            ["reviewer_b"]="reviewer_b"
            ["reviewerb"]="reviewer_b"
            ["revb"]="reviewer_b"
            ["summarizer"]="summarizer"
            ["summary"]="summarizer"
            ["observer"]="observer"
            ["echo"]="echo"
            ["test"]="test"
          )

          # 查找匹配的内置 Agent（不区分大小写）
          FOUND_AGENTS=""
          for alias in "${!AGENT_MAP[@]}"; do
            if echo "$COMMENT" | grep -qi "@${alias}"; then
              AGENT="${AGENT_MAP[$alias]}"
              # 去重添加
              if [ -z "$FOUND_AGENTS" ]; then
                FOUND_AGENTS="$AGENT"
              elif [[ ",$FOUND_AGENTS," != *",$AGENT,"* ]]; then
                FOUND_AGENTS="$FOUND_AGENTS,$AGENT"
              fi
            fi
          done

          if [ -n "$FOUND_AGENTS" ]; then
            echo "found=true" >> $GITHUB_OUTPUT
            echo "agents=$FOUND_AGENTS" >> $GITHUB_OUTPUT
            echo "Found built-in agents: $FOUND_AGENTS"
          else
            echo "found=false" >> $GITHUB_OUTPUT
            echo "No built-in agents found, will be handled by dispatch_agents.yml"
          fi

      # 如果找到内置 Agent，在主仓库执行
      - name: Run built-in agents
        if: steps.check-agents.outputs.found == 'true'
        run: |
          echo "🚀 Running built-in agents: ${{ steps.check-agents.outputs.agents }}"

          uv run python -m issuelab execute \
            --issue ${{ github.event.issue.number }} \
            --agents "${{ steps.check-agents.outputs.agents }}" \
            --post
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_AUTH_KEY: ${{ secrets.ANTHROPIC_AUTH_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          ARXIV_STORAGE_PATH: ${{ github.workspace }}/.arxiv-papers
          LOG_FILE: ${{ github.workspace }}/logs/mention_${{ github.event.issue.number }}.log
          LOG_LEVEL: DEBUG
          ENABLE_ARXIV_MCP: true
          ENABLE_GITHUB_MCP: false
          CLAUDE_AGENT_SDK_SKIP_VERSION_CHECK: "true"
          CLAUDE_CODE_STREAM_CLOSE_TIMEOUT: "30000"

      # 上传日志文件
      - name: Upload logs
        if: always() && steps.check-agents.outputs.found == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: mention-logs-${{ github.event.issue.number }}-${{ github.run_id }}
          path: logs/
          retention-days: 7
          if-no-files-found: ignore

  # ========== /command 触发（顺序执行） ==========
  process-command:
    if: github.event_name == 'issue_comment' &&
        (contains(github.event.comment.body, '/review') ||
         contains(github.event.comment.body, '/summarize') ||
         contains(github.event.comment.body, '/quiet') ||
         contains(github.event.comment.body, '/triage') ||
         contains(github.event.comment.body, '/echo')) &&
        !contains(github.event.comment.body, '@')
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true
          cache-dependency-glob: "**/pyproject.toml"

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync

      - name: Cache arxiv-mcp-server
        uses: actions/cache@v4
        with:
          path: ~/.local/share/uv/tools/arxiv-mcp-server
          key: arxiv-mcp-${{ runner.os }}-${{ hashFiles('pyproject.toml') }}
          restore-keys: |
            arxiv-mcp-${{ runner.os }}-

      - name: Extract command
        id: extract
        run: |
          COMMENT="${{ github.event.comment.body }}"
          if echo "$COMMENT" | grep -q '/review'; then
            echo "command=/review" >> $GITHUB_OUTPUT
          elif echo "$COMMENT" | grep -q '/summarize'; then
            echo "command=/summarize" >> $GITHUB_OUTPUT
          elif echo "$COMMENT" | grep -q '/quiet'; then
            echo "command=/quiet" >> $GITHUB_OUTPUT
          elif echo "$COMMENT" | grep -q '/triage'; then
            echo "command=/triage" >> $GITHUB_OUTPUT
          elif echo "$COMMENT" | grep -q '/echo'; then
            echo "command=/echo" >> $GITHUB_OUTPUT
          fi

      # 处理 /quiet 命令
      - name: Handle quiet command
        if: steps.extract.outputs.command == '/quiet'
        run: |
          gh issue edit ${{ github.event.issue.number }} \
            --add-label "bot:quiet" \
            --repo ${{ github.repository }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 处理 /triage 命令：仅运行 Moderator 分诊
      - name: Handle triage command
        if: steps.extract.outputs.command == '/triage'
        run: |
          uv run python -m issuelab execute \
            --issue ${{ github.event.issue.number }} \
            --agents "moderator" \
            --post
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_AUTH_KEY: ${{ secrets.ANTHROPIC_AUTH_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          ARXIV_STORAGE_PATH: ${{ github.workspace }}/.arxiv-papers
          LOG_FILE: ${{ github.workspace }}/logs/triage_${{ github.event.issue.number }}.log
          LOG_LEVEL: DEBUG
          ENABLE_ARXIV_MCP: true
          ENABLE_GITHUB_MCP: false
          CLAUDE_AGENT_SDK_SKIP_VERSION_CHECK: "true"
          CLAUDE_CODE_STREAM_CLOSE_TIMEOUT: "30000"

      # 处理 /review 命令：运行完整评审流程
      - name: Run review process
        if: steps.extract.outputs.command == '/review'
        run: |
          echo "🏷️ /review 命令触发评审"
          gh issue edit ${{ github.event.issue.number }} \
            --add-label "state:review" \
            --repo ${{ github.repository }}

          uv run python -m issuelab review \
            --issue ${{ github.event.issue.number }} \
            --post

          echo "📝 标记需要总结..."
          gh issue edit ${{ github.event.issue.number }} \
            --add-label "bot:needs-summary" \
            --repo ${{ github.repository }}

          echo "✅ 评审完成"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_AUTH_KEY: ${{ secrets.ANTHROPIC_AUTH_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          ARXIV_STORAGE_PATH: ${{ github.workspace }}/.arxiv-papers
          LOG_FILE: ${{ github.workspace }}/logs/review_${{ github.event.issue.number }}.log
          LOG_LEVEL: DEBUG
          ENABLE_ARXIV_MCP: true
          ENABLE_GITHUB_MCP: false
          CLAUDE_AGENT_SDK_SKIP_VERSION_CHECK: "true"
          CLAUDE_CODE_STREAM_CLOSE_TIMEOUT: "30000"

      # 处理 /summarize 命令
      - name: Run summarizer
        if: steps.extract.outputs.command == '/summarize'
        run: |
          uv run python -m issuelab execute \
            --issue ${{ github.event.issue.number }} \
            --agents "summarizer" \
            --post
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_AUTH_KEY: ${{ secrets.ANTHROPIC_AUTH_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          ARXIV_STORAGE_PATH: ${{ github.workspace }}/.arxiv-papers
          LOG_FILE: ${{ github.workspace }}/logs/summarize_${{ github.event.issue.number }}.log
          LOG_LEVEL: DEBUG
          ENABLE_ARXIV_MCP: true
          ENABLE_GITHUB_MCP: false
          CLAUDE_AGENT_SDK_SKIP_VERSION_CHECK: "true"
          CLAUDE_CODE_STREAM_CLOSE_TIMEOUT: "30000"

      # 处理 /echo 命令 - 快速测试
      - name: Run echo test
        if: steps.extract.outputs.command == '/echo'
        run: |
          uv run python -m issuelab execute \
            --issue ${{ github.event.issue.number }} \
            --agents "echo" \
            --post
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_AUTH_KEY: ${{ secrets.ANTHROPIC_AUTH_KEY }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
          LOG_FILE: ${{ github.workspace }}/logs/echo_${{ github.event.issue.number }}.log
          LOG_LEVEL: DEBUG
          ENABLE_ARXIV_MCP: false
          ENABLE_GITHUB_MCP: false
          CLAUDE_AGENT_SDK_SKIP_VERSION_CHECK: "true"
          CLAUDE_CODE_STREAM_CLOSE_TIMEOUT: "30000"

      # 上传日志文件
      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orchestrator-logs-${{ github.event.issue.number }}-${{ github.run_id }}
          path: logs/
          retention-days: 7
          if-no-files-found: ignore
